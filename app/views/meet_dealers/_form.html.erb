<%= form_for(@meet_dealer) do |f| %>
  <% if @meet_dealer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@meet_dealer.errors.count, "error") %> prohibited this meet_dealer from being saved:</h2>

      <ul>
      <% @meet_dealer.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :purpose %><br>
    <%= f.text_field :purpose %>
  </div>
  <div class="field">
    <%= f.label :other %><br>
    <%= f.text_area :other %>
  </div>
  <div class="field">
    <%= f.label :sr_activity_id %><br>
    <%= f.number_field :sr_activity_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
