<%= form_for(@pocket_district) do |f| %>
  <% if @pocket_district.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@pocket_district.errors.count, "error") %> prohibited this pocket_district from being saved:</h2>

      <ul>
      <% @pocket_district.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :pocket_id %><br>
    <%= f.number_field :pocket_id %>
  </div>
  <div class="field">
    <%= f.label :district_id %><br>
    <%= f.number_field :district_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
