<%= form_for(@single) do |f| %>
  <% if @single.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@single.errors.count, "error") %> prohibited this single from being saved:</h2>

      <ul>
      <% @single.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
